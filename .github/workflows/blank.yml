导入日期时间
导入请求
导入json

while True:
current_time=datetime.datetime.now()
如果current_time.hour==8，current_time.minute==0，current_time.second==0:
URL="https://a1.fanbook.mobi/api/bot/机器人令牌/SendMessage"
headers={'content-type':"application/json；charset=utf-8"}
JsonFile=json.dumps({
"chat_id":"频道ID(证书)",
"文本":"早上好"
        })
postreturn=requests.post(url，data=JsonFile，headers=headers)
打印(postreturn.text)
Elif current_time.hour==20和current_time.minute==0和current_time.second==0:
URL="https://a1.fanbook.mobi/api/bot/机器人令牌/SendMessage"
headers={'content-type':"application/json；charset=utf-8"}
JsonFile=json.dumps({
"chat_id":"频道ID(证书)",
"文本":"晚上好"
        })
postreturn=requests.post(url，data=JsonFile，headers=headers)
打印(postreturn.text)





















